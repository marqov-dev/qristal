# ANTLR wheel inputs

The exact acquired Python runtime source and the two pinned build-tool wheels
passed input verification. Source was staged without executing setup.py or
installing anything. The recipe/source identities are retained here. Four
offline verifier tests pass. No wheel has been built by this preparation;
Linux CPython3.10 build and parser-runtime checks remain required.
