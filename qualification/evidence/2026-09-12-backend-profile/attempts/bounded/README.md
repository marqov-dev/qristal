# Second attempt: evidence payload limit confirmed

Source `18e5ca5` completed the recorded build stages, QFT stage and six-case input
stage, then the full fixture reached `ProcessError:process_timeout`. The guest's
compact diagnostic reports 70,286 raw report bytes and 20,828 encoded characters:
348 characters above the fixed 20,480-character console envelope. Full timing
rows and mathematical vectors were not retained, so these stage statuses do not
replace independently replayable correctness or profiling evidence.

The exact VM, disk, group and transfer cleanup checks passed. The next separately
declared attempt omits redundant command arrays (available in pinned guest source),
uses maximum zlib compression and emits progress every 131,072 visited nodes.
Neither process/fixture limits nor console-envelope limits change.
