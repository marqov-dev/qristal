# Core native runner preparation

The exact public-input package was assembled and verified. Archive and protocol
hashes are retained here; the full archive remains local. All selected offline
suites passed, including 12 native-runner/classifier/material tests, 16 selection
and baseline tests, and 8 output-retention tests. No native success is claimed.

Automatic approval review rejected the attempted Core launch before process
creation because this exact package and AWS destination require explicit user
authorization. No experiment state directory or AWS resources were created.
This is a pending execution gate, not a failed native build.

A future approved run must use the recorded package or explicitly prepare and
record a new identity. Do not bypass the approval or relabel source-only checks
as native evidence. The existing successful XACC artifact remains unchanged.
